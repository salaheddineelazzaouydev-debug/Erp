# ERP SaaS Project Structure

- `backend/` with apps: `core`, `accounts`, `finance`, `crm`, `inventory`, `hr`, `api`, `config`, plus `manage.py`.
- `frontend/` with `app`, `components`, and `lib`.
- `infra/` with `docker`, `nginx`, and `k8s`.
- `scripts/` for automation helpers.
